global _start
section .text
_start:
	mov ax,7510	;16-bit num (256-65k)
	mov bl,75	;8-bit num (<=256)
	div bl		
			;AL = quotient
			;AH = remainder
	
	mov cl,ah
	xor ah,ah	
			
	mov eax,1
	xor ebx,ebx
	int 80h
